{
  "private": true,
  "scripts": {
    "prepare": "husky",
    "build": "pnpm --filter @bamboo-pan/trellis build",
    "test": "pnpm --filter @bamboo-pan/trellis test",
    "lint": "pnpm --filter @bamboo-pan/trellis lint",
    "typecheck": "pnpm --filter @bamboo-pan/trellis typecheck",
    "release": "pnpm --filter @bamboo-pan/trellis release",
    "release:minor": "pnpm --filter @bamboo-pan/trellis release:minor",
    "release:beta": "pnpm --filter @bamboo-pan/trellis release:beta",
    "release:rc": "pnpm --filter @bamboo-pan/trellis release:rc"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^16.2.7"
  }
}
